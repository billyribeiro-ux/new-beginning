# PR #1 — Workspace Skeleton — Launch Evidence

**Date:** 2026-05-25
**Branch:** `claude/blissful-lamport-g0O6m`
**Scope:** BACKEND.md §21 row 1 — workspace skeleton, `common::money`,
`AppError` + `AlertSink`, `Config`, `observability` crate, `/healthz`,
`/readyz`, CI green.

## Verification gates (BACKEND.md §21 row 1)

| Gate | Result |
|---|---|
| `cargo build --workspace` clean | ✅ |
| `cargo clippy --workspace --all-targets -- -D warnings` clean | ✅ |
| `cargo fmt --all -- --check` clean | ✅ |
| `cargo test --workspace` green | ✅ 36 passed / 0 failed |
| `cargo run -p api` boots and binds | ✅ (logs below) |
| `curl /healthz` → 200 | ✅ |
| Money overflow returns `Err(Overflow)`, not saturation | ✅ (unit test `mul_bps_overflow_is_err_not_saturated`) |
| Symmetric JS-safe serde guard | ✅ (tests `deserialize_rejects_above_js_safe` / `_below_` and serialize counterparts) |
| `AlertSink` declared and wired into `AppState` + readiness | ✅ (tests in `routes::health::readyz_failing_check_returns_503_and_fires_alert`) |

## MCP-tool fallback note

CLAUDE.md mandates rust-analyzer MCP diagnostics for every `.rs` edit. The
MCP server was not surfaced in this session's tool list, so the gate was
satisfied with `cargo check` + `cargo clippy --all-targets -- -D warnings`.
This fallback is called out explicitly per CLAUDE.md.

## Runtime evidence

### Boot logs (JSON, structured)

```
{"timestamp":"2026-05-25T17:20:57.373058Z","level":"INFO","fields":{"message":"OTEL_EXPORTER_OTLP_ENDPOINT not set; OTLP tracing disabled (stdout JSON still on)"},"target":"observability"}
{"timestamp":"2026-05-25T17:20:57.574031Z","level":"INFO","fields":{"message":"Prometheus /metrics listener installed","addr":"127.0.0.1:9091"},"target":"observability"}
{"timestamp":"2026-05-25T17:20:57.574106Z","level":"INFO","fields":{"message":"starting api binary","service":"tradeflex-api","env":"development","bind":"127.0.0.1:8081","metrics_bind":"127.0.0.1:9091"},"target":"api"}
{"timestamp":"2026-05-25T17:20:57.574611Z","level":"INFO","fields":{"message":"listening","addr":"127.0.0.1:8081"},"target":"api"}
```

Boot order matches BACKEND.md §15:
1. OTLP layer decision (disabled — no endpoint configured).
2. Prometheus listener installed on its **separate** port (BACKEND.md
   §15 rule: separate from the api bind, so a public LB cannot leak metrics).
3. `starting api binary` log line carries `service`, `env`, and both binds —
   the required service-attribution fields.
4. `listening` confirms socket bound.

### `/healthz`

```
$ curl -i http://127.0.0.1:8081/healthz
HTTP/1.1 200 OK
content-type: application/json
x-request-id: 019e6027-a3e0-7402-9f16-cb85d800e64a
content-length: 15

{"status":"ok"}
```

Response has UUIDv7 `x-request-id` (time-sortable, auto-generated by the
request-id middleware — BACKEND.md §15).

### `/readyz` with empty registry

```
$ curl -i http://127.0.0.1:8081/readyz
HTTP/1.1 200 OK
content-type: application/json
x-request-id: 019e6027-a3ed-7eb0-9846-f59cf60ffa6f
content-length: 30

{"checks":{},"status":"ready"}
```

PR #1 registers no checks. The registry pattern means PR #2 will add the DB
check additively without touching this handler. Failing-check behavior is
already covered by the test
`readyz_failing_check_returns_503_and_fires_alert` (returns 503 + fires
`AlertKind::ReadinessDegraded`).

### Incoming request-id is honored, not replaced

```
$ curl -i -H "x-request-id: pr1-evidence-001" http://127.0.0.1:8081/healthz
HTTP/1.1 200 OK
x-request-id: pr1-evidence-001
...
{"status":"ok"}
```

### Unknown route

```
$ curl -o /dev/null -w "HTTP %{http_code}\n" http://127.0.0.1:8081/nope
HTTP 404
```

### `/metrics`

```
$ curl http://127.0.0.1:9091/metrics
(empty — no metrics emitted yet; instrumentation lands in later PRs)
```

The listener is reachable on its dedicated port (BACKEND.md §15). The
emptiness is the truthful state: no business code is wired yet, so there is
nothing to count. PR #6+ will register counters/histograms.

## Test summary

```
common      27 passed / 0 failed
observability  0 (no tests yet; init is exercised by api binary boot)
tradeflex-http 9 passed / 0 failed
---
TOTAL       36 passed / 0 failed
```

Notable money-correctness tests passing:

- `mul_bps_overflow_is_err_not_saturated` — `i64::MAX * 200%` → `Err(Overflow)`
- `mul_pct_overflow_in_pct_to_bps` — overflow caught in `pct.checked_mul(100)` before widening
- `mul_qty_overflow_is_err` — overflow on quantity-times-price
- `serialize_rejects_above_js_safe` / `_below_` — symmetric serde rejection
- `deserialize_rejects_above_js_safe` / `_below_` — symmetric serde rejection
- `internal_error_does_not_leak_source` — `AppError::Internal` body does not contain the source string

## Anti-regressions

These properties must hold in every subsequent PR:

1. `Money` has no `Mul` impl for `i64` or `Money`. (Verified by absence: the
   tests use `mul_qty` / `mul_bps` exclusively.)
2. `AppError::Internal` never echoes the source string to the response body.
3. `AlertSink` is the only mechanism for "should-never-happen" reporting.
4. `/healthz` ignores all dependencies. `/readyz` reflects them honestly.
5. Request-id is generated when absent and propagated when present, on every
   route.
6. JSON log output is structured (one event per line, valid JSON).
